---
/**
 * Post Component
 * 
 * This component displays a blog post with a title, description, publication date, and author.
 * It also includes a slot for additional content to be inserted.
 * 
 * Props:
 * @property {string} title - The title of the post.
 * @property {string} description - The description of the post.
 * @property {string} pubDate - The publication date of the post.
 * @property {string} author - The author of the post.
 * @property {Array<string>} tags - An array of tags for the post.
 */

const { title, description, pubDate, author, tags } = Astro.props;
---

<article>
  <h1>{title}</h1>
  <p class="post-tags">
    {tags.map(tag => (
      <a href={`/tags/${tag}`} class="tag-link">#{tag}</a>
    ))}
  </p> <!-- Generated by Copilot -->
  <p>{description}</p>
  <p>Published on: {pubDate}</p>
  <p>Author: {author}</p>

  <slot></slot>
</article>